spring:
  application:
    name: Plan University Schedule
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name:
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  liquibase:
    enabled: false